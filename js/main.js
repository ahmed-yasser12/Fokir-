var typed = new Typed(".type", {
    strings: [" Designer ", "Developer"],
    typeSpeed: 90, // Default value
    backSpeed: 110, // Default value
    loop: true, // Default value
  });

let navbar =document.getElementById("navBar");
console.log(navbar);
